---
title: 紀錄git error
tags: ["code"]
---

## 更新被拒絕，因為您目前分支的最新提交落後於其對應的遠端分支。

1. 先從遠端fetch後merage再push

```shell
git fetch origin
git merge origin/master
git push
```

2. 強制push

```shell
git push -f
```
